<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardBackgroundColor="@color/item"
    app:cardCornerRadius="2dp"
    app:cardElevation="10dp"
    android:layout_margin="2dp"
    android:id="@+id/profile_top_id">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="10dp"
        android:orientation="vertical"
        android:gravity="center">

        <LinearLayout
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="15dp"
            android:background="@drawable/person_icon_bg"
            android:gravity="center"
            android:id="@+id/profile_icon_bg_id">

            <de.hdodenhof.circleimageview.CircleImageView
                android:layout_width="90dp"
                android:layout_height="90dp"
                android:src="@drawable/flower"
                android:id="@+id/profile_main_img_id"/>

        </LinearLayout>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dp"
            android:layout_gravity="center_horizontal"
            android:text="گل بنفسه"
            android:textColor="@color/white"
            android:fontFamily="@font/b_titr_bold"
            android:textSize="16dp"
            android:id="@+id/profile_name_id"/>

    </LinearLayout>

</androidx.cardview.widget.CardView>